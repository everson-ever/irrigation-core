---
status: backlog
priority: high
type: maintenance
---

## Title

Require replacement of factory-default credentials

## Specification

Replace the known `admin/10203040` steady-state policy with a safe provisioning or
forced-first-change flow. Review the local `reset-to-default` recovery command so a
reset cannot leave the network dashboard indefinitely usable with a known password.
Preserve a documented local recovery path and add end-to-end tests.

